# 기본설정, 버전은 build.gradle 파일 version 참조
spring.application.name=spring-board-app
spring.application.version=${version}
server.servlet.context-path=/

# 타임리프 템플릿 경로 설정
spring.thymeleaf.prefix=classpath:/templates

# 파일 업로드 설정
spring.servlet.multipart.enabled=true
spring.servlet.multipart.location=uploads
spring.servlet.multipart.max-file-size=10MB




# Mail 설정
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=cjy525147@gmail.com
spring.mail.password=yexqisrfszqfzmtj
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com


# DataSource(커넥션풀) 설정
spring.datasource.url=jdbc:mysql://15.165.49.85:3306/board
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=board
spring.datasource.password=Chltnqls5251!


#Mybatis 설정
mybatis.mapper-locations=classpath:mappers/**/*.xml

# JPA 설정
# ddl-auto=create : 엔티티를 기준으로 기존 테이블 삭제 후 다시 생성
# ddl-auto=update : 엔티티를 기준으로 기존 테이블 수정(개발용)
# ddl-auto=none : 엔티티를 기준으로 테이블 생성하지 않음(배포용)
spring.jpa.database=mysql
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.show_sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# LOG 설정
logging.level.root=info
logging.file.name=/logs/spring-board-app.log
